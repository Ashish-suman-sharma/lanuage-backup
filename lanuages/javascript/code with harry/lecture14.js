// string methods 

let name = "ashish"
let friend = "suman"
let friend2 = "   rahul   "
console.log(name.length);

console.log(name.toUpperCase());
console.log(name.toLowerCase());
console.log(name.slice(2,4));
console.log(name.replace("a","b"));
console.log(name.charAt(2));
console.log(name.indexOf("a"));
console.log(name.concat(" "+friend));
console.log(friend2.trim());
console.log(name[3])

//uses

//length is used to find length of string
//toUpperCase is used to convert string into uppercase
//toLowerCase is used to convert string into lowercase
//slice is used to slice string from given index    
//replace is used to replace character in string
//charAt is used to find character at given index
//indexOf is used to find index of given character in string
//concat is used to concatinate two string
//trim is used to remove extra spaces from string
//[] is used for finding the aplhabet at a given index
//we can also use string methods on string literals